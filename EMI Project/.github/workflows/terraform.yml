name: Terraform CI/CD

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
      - develop
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

jobs:
  validate:
    name: Validate Terraform
    runs-on: ubuntu-latest
    strategy:
      matrix:
        environment: [dev, staging, prod]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.7.5

      - name: Terraform fmt
        run: terraform fmt -check -recursive

      - name: Terraform init
        working-directory: ./EMI Project/environments/${{ matrix.environment }}
        run: terraform init -backend=false

      - name: Terraform validate
        working-directory: ./EMI Project/environments/${{ matrix.environment }}
        run: terraform validate

  plan-dev:
    name: Plan dev
    needs: validate
    if: github.event_name == 'pull_request' || github.ref == 'refs/heads/develop' || github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    environment:
      name: dev
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::123456789012:role/emi-dev-deploy-role
          aws-region: us-east-1

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.7.5

      - name: Terraform init with backend
        working-directory: ./EMI\ Project/environments/dev
        run: terraform init -backend-config=backend.hcl

      - name: Terraform plan
        working-directory: ./EMI\ Project/environments/dev
        run: terraform plan -no-color -out=tfplan

  plan-staging:
    name: Plan staging
    needs: validate
    if: github.event_name == 'pull_request' || github.ref == 'refs/heads/develop' || github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    environment:
      name: staging
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::123456789012:role/emi-staging-deploy-role
          aws-region: us-east-1

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.7.5

      - name: Terraform init with backend
        working-directory: ./EMI\ Project/environments/staging
        run: terraform init -backend-config=backend.hcl

      - name: Terraform plan
        working-directory: ./EMI\ Project/environments/staging
        run: terraform plan -no-color -out=tfplan

  apply-prod:
    name: Apply prod
    needs: plan-staging
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    runs-on: ubuntu-latest
    environment:
      name: prod
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::123456789012:role/emi-prod-deploy-role
          aws-region: us-east-1

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.7.5

      - name: Terraform init with backend
        working-directory: ./EMI\ Project/environments/prod
        run: terraform init -backend-config=backend.hcl

      - name: Terraform plan
        working-directory: ./EMI\ Project/environments/prod
        run: terraform plan -no-color -out=tfplan

      - name: Terraform apply
        working-directory: ./EMI\ Project/environments/prod
        run: terraform apply -auto-approve tfplan
